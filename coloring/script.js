// coloring/script.js
document.addEventListener('DOMContentLoaded', () => {
    // Optional: add interactivity for the coloring page options if needed.
    // Currently, the page simply links to the individual coloring pages.
  });
